import Header from '@/Components/Header';
import React from 'react';

const Test = () => {
  const auth = {
  };

  return (
    <div>
      <Header auth={auth} />
    </div>
  );
};

export default Test;
